{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://taskbarwidgets.dev/schemas/widget-v3.schema.json",
  "title": "Taskbar Widgets Web Manifest v3",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "id", "version", "minHostVersion", "displayName", "description", "author", "size", "renderer", "permissions"],
  "properties": {
    "$schema": { "type": "string" },
    "schemaVersion": { "const": 3 },
    "id": { "type": "string", "pattern": "^[a-z][a-z0-9.-]{2,127}\\.[a-z0-9.-]+$" },
    "version": { "type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$" },
    "minHostVersion": { "type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$" },
    "displayName": { "type": "string", "minLength": 1, "maxLength": 80 },
    "description": { "type": "string", "minLength": 1, "maxLength": 500 },
    "category": { "type": "string", "maxLength": 40 },
    "author": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name"],
      "properties": {
        "name": { "type": "string", "minLength": 1, "maxLength": 80 },
        "website": { "type": "string", "format": "uri", "pattern": "^https://" }
      }
    },
    "size": {
      "type": "object",
      "additionalProperties": false,
      "required": ["width", "height"],
      "properties": {
        "width": { "type": "integer", "minimum": 32, "maximum": 480 },
        "height": { "type": "integer", "minimum": 24, "maximum": 48 }
      }
    },
    "renderer": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "entry"],
      "properties": {
        "type": { "const": "web" },
        "entry": { "type": "string", "pattern": "^(?!.*\\.\\.)[^\\\\]+\\.html$" },
        "expandedSize": {
          "type": "object",
          "additionalProperties": false,
          "required": ["width", "height"],
          "properties": {
            "width": { "type": "integer", "minimum": 32, "maximum": 640 },
            "height": { "type": "integer", "minimum": 24, "maximum": 480 }
          }
        },
        "expandDirection": { "enum": ["auto", "up", "down"], "default": "auto" },
        "activation": { "enum": ["hover", "click", "manual"], "default": "hover" },
        "hoverDelayMs": { "type": "integer", "minimum": 0, "maximum": 1000, "default": 100 },
        "collapseDelayMs": { "type": "integer", "minimum": 0, "maximum": 2000, "default": 180 },
        "transitionMs": { "type": "integer", "minimum": 0, "maximum": 500, "default": 280 }
      }
    },
    "entry": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "provider": {
          "type": "object",
          "required": ["type"],
          "properties": {
            "type": { "enum": ["static", "clock", "http-json", "javascript"] },
            "path": { "type": "string", "pattern": "^[^/\\\\]+\\.js$" },
            "url": { "type": "string", "format": "uri", "pattern": "^https://" },
            "refreshSeconds": { "type": "number", "minimum": 1, "maximum": 3600 },
            "data": { "type": "object" }
          }
        }
      }
    },
    "permissions": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "network": { "type": "array", "uniqueItems": true, "items": { "type": "string", "format": "hostname" } },
        "systemMetrics": { "type": "array", "uniqueItems": true, "items": { "enum": ["cpu", "storage", "network", "memory"] } },
        "openExternal": { "type": "array", "items": { "type": "string", "format": "hostname" } },
        "storage": { "type": "boolean" },
        "graphics": { "type": "array", "uniqueItems": true, "items": { "enum": ["webgl", "continuousAnimation"] } }
      }
    },
    "supportsMultipleInstances": { "type": "boolean", "default": false },
    "settings": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": true,
        "required": ["key", "type", "label", "default"],
        "properties": {
          "key": { "type": "string", "pattern": "^[A-Za-z][A-Za-z0-9_]{0,63}$" },
          "type": { "enum": ["string", "number", "boolean", "select", "secret"] },
          "label": { "type": "string", "maxLength": 80 },
          "description": { "type": "string", "maxLength": 240 },
          "default": {},
          "minimum": { "type": "number" },
          "maximum": { "type": "number" },
          "step": { "type": "number", "exclusiveMinimum": 0 }
        }
      }
    }
  }
}
